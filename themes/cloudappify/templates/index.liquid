<section class="pie-clearfix" id="index">
  <section id="plans">
    {% for product in collections.frontpage.products limit: 1 %}
      <hgroup>
        <h1>{{ product.title }}</h1>
      </hgroup>

      <form action="/cart/add" method="post">
        <fieldset>
          {% for variant in product.variants %}
            {% if variant.available %}
              <label><input {% if forloop.first %}checked {% endif %}id="pro-{{ variant.title | replace: ' ', '-' }}" name="id" type="radio" value="{{ variant.id }}" /> {{ variant.title | escape }} &mdash; {{ variant.price | money }}</label>
            {% endif %}
          {% endfor %}
        </fieldset>

        <p>
          <input class="green" type="submit" value="Get {{ product.title }}" />
        </p>
      </form>

      <script>
        (function($) {
          $(function() {
            var hash = window.location.hash.replace(/#/, '');
            $("#pro-" + hash).attr("checked", "checked");
          });
        }(jQuery));
      </script>
    {% endfor %}
  </section>

  <section id="description">
    <div class="column">
      <h2>Turbocharge Your Account</h2>

      <p>
        Experience the heavenliness of CloudApp Pro and be blown away by:
      </p>

      <ul>
        <li>Unlimited uploads (Free: 10 uploads per day)</li>
        <li>Upload files up to 250MB (Free: 25MB per file)</li>
        <li>Customize your links with your own domain</li>
      </ul>

<!--
      <p>
        CloudApp is free forever. There are no expiring trials, hidden charges or contracts. However if you want to share more than 10 uploads a day (25MB max. file size) you should turbocharge your account with CloudApp Pro:
      </p>

      <ul>
        <li>Unlimited Daily Uploads</li>
        <li>Up To 250MB Per Upload</li>
        <li>Use Your Own Domain</li>
      </ul>
-->
    </div>

    <div class="column">
      <h2>How Does It Work?</h2>

      <p>
        Snag a CloudApp Pro Gift Card for yourself or a friend and redeem it with a single click.  Your account will be empowered and you can enjoy all the benefits of CloudApp Pro.
      </p>

      <p>
        You won't be cheated by hidden fees or confused by contracts. Simply pay as you go.  Don't worry. We'll let you know when your gift card is about to expire.
      </p>
    </div>

    <div class="column">
      <h2>We love questions!</h2>

      <p>
        Have a question about a CloudApp Pro feature, how gift cards work, or life after the Singularity? <a href="mailto:support@getcloudapp.com">Let us know!</a>
      </p>
    </div>
  </section>
</section>
